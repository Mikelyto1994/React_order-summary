import { defineConfig } from "vite";

// https://vitejs.dev/config/
export default defineConfig({
  base: "/React_order-summary/", // Reemplaza con el nombre de tu repositorio
  build: {
    outDir: "dist", // Directorio de salida para la construcción
  },
});
